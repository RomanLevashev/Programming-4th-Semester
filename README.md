# Programming Projects - 3rd Semester

Repository for programming assignments in the 4th semester. All projects are built using .NET 10.0.

## Build and Run


```bash
# Build first
dotnet build <PATH_TO_SOLUTION>

# Then run
dotnet run --project <PATH_TO_PROJECT>
```

**For specific configuration:**
```bash
dotnet build --configuration Release
```